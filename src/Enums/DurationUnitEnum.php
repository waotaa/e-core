<?php

namespace Vng\EvaCore\Enums;

use MyCLabs\Enum\Enum;

class DurationUnitEnum extends Enum
{
    private const hour = 'Uur';
    private const day = 'Dag(en)';
    private const week = 'We(e)k(en)';
    private const month = 'Maand(en)';
}
