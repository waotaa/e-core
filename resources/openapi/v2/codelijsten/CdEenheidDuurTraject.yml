type: string
enum:
  - "01"  # Dag(en)
  - "02"  # We(e)k(en)
  - "03"  # Maand(en)
  - "04"  # Uur
description: |
  Codelijst voor traject duur eenheden:
  - "01": Dag(en)
  - "02": We(e)k(en)
  - "03": Maand(en)
  - "04": Uur
