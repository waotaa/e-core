type: object
properties:
  TitelNieuwsbericht:
    type: string
    maxLength: 200
    pattern: "^[a-zA-Z0-9]*$"
  OndertitelNieuwsbericht:
    type: string
    maxLength: 200
    pattern: "^[a-zA-Z0-9]*$"
  InhoudNieuwsbericht:
    type: string
    maxLength: 10000
    pattern: "^[a-zA-Z0-9]*$"
  TeaserNieuwsbericht:
    type: string
    maxLength: 10000
    pattern: "^[a-zA-Z0-9]*$"
    nullable: true
  IndPublicatieNieuwsbericht:
    $ref: '../codelijsten/StdIndJN.yml'
  DatBPublicatieNieuwsbericht:
    type: string
    format: date-time
    nullable: true
  DatEPublicatieNieuwsbericht:
    type: string
    format: date-time
    nullable: true
  DatPublicatieNieuwsbericht:
    type: string
    format: date-time
  Bewerkmoment:
    $ref: './bewerkmoment.yml'
  InstrumentOmgeving:
    $ref: './instrumentOmgeving.yml'