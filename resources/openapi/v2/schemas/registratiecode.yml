type: object
properties:
  Registratiecode:
    type: string
    maxLength: 34
    pattern: "^[a-zA-Z0-9]*$"
  RegistratiecodeLabel:
    type: string
    maxLength: 200
    pattern: "^[a-zA-Z0-9]*$"
    nullable: true
  IndWeergeven:
    $ref: '../codelijsten/StdIndJN.yml'