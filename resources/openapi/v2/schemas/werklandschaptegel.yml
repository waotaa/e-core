type: object
properties:
  CdWerklandschaptegel:
    $ref: '../codelijsten/CdWerklandschaptegel.yml'
  NaamWerklandschaptegel:
    type: string
    maxLength: 200
    pattern: "^[a-zA-Z0-9]*$"
  ToelNaamWerklandschaptegel:
    type: string
    maxLength: 200
    pattern: "^[a-zA-Z0-9]*$"
  SlugWerklandschaptegel:
    type: string
    maxLength: 200
    pattern: "^[a-zA-Z0-9]*$"
  KorteOmsWerklandschaptegel:
    type: string
    maxLength: 2000
    pattern: "^[a-zA-Z0-9]*$"
    nullable: true
  OmsWerklandschaptegel:
    type: string
    maxLength: 10000
    pattern: "^[a-zA-Z0-9]*$"
    nullable: true